import { createFileRoute } from "@tanstack/react-router";
import { AlertTriangle, Car, IndianRupee, Users } from "lucide-react";
import { formatCurrency, sampleBookings, vehicles } from "@/lib/rental-data";
export const Route = createFileRoute("/admin")({ head: () => ({ meta: [{ title: "Fleet Operations — Velocity Fleet" },{ name: "description", content: "Read-only Velocity Fleet operations dashboard for vehicles, bookings and payments." },{ property: "og:title", content: "Velocity Fleet operations" },{ property: "og:description", content: "Fleet availability, active rentals and payment overview." },{ property: "og:type", content: "website" },{ name: "twitter:card", content: "summary_large_image" }] }), component: AdminPage });
function AdminPage(){const revenue=sampleBookings.reduce((s,b)=>s+b.paid,0);return <main className="mx-auto min-h-[75vh] max-w-7xl px-5 py-10 sm:px-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Operations console</p><h1 className="mt-2 font-display text-5xl text-ink">FLEET CONTROL</h1><p className="mt-2 text-muted">Read-only demo · updated 20 Sep, 20:23 IST</p></div><span className="rounded-full bg-ok/15 px-3 py-1.5 font-mono text-[10px] uppercase tracking-[0.16em] text-ok">All systems operational</span></div><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[[Car,"Total vehicles","4"],[Users,"Live bookings","2"],[IndianRupee,"Payments received",formatCurrency(revenue)],[AlertTriangle,"In maintenance","1"]].map(([Icon,label,value])=>{const C=Icon as typeof Car;return <div key={String(label)} className="rounded-card border border-line bg-panel p-5"><C className="size-5 text-brand"/><p className="mt-5 text-xs text-muted">{String(label)}</p><p className="mt-1 text-2xl font-semibold text-ink">{String(value)}</p></div>})}</div><div className="mt-8 grid gap-6 lg:grid-cols-[1.25fr_0.75fr]"><section className="overflow-hidden rounded-card border border-line bg-panel"><div className="border-b border-line p-5"><p className="eyebrow">Active & upcoming</p><h2 className="mt-1 text-lg font-semibold text-ink">Booking queue</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[620px] text-left text-sm"><thead className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted"><tr>{["Ref","Customer","Vehicle","Dates","Status","Due"].map(x=><th key={x} className="px-5 py-3 font-medium">{x}</th>)}</tr></thead><tbody>{sampleBookings.slice(0,2).map(b=><tr key={b.reference} className="border-t border-line"><td className="px-5 py-4 font-mono text-xs text-brand">{b.reference}</td><td className="px-5 py-4 text-ink">{b.customer}</td><td className="px-5 py-4 text-muted">{b.vehicle}</td><td className="px-5 py-4 text-muted">{b.startDate}<br/>{b.endDate}</td><td className="px-5 py-4"><span className="text-ok">{b.status}</span></td><td className="px-5 py-4 text-ink">{formatCurrency(b.total-b.paid)}</td></tr>)}</tbody></table></div></section><section className="rounded-card border border-line bg-panel p-5"><p className="eyebrow">Fleet status</p><h2 className="mt-1 text-lg font-semibold text-ink">Vehicle readiness</h2><div className="mt-5 space-y-4">{vehicles.map(v=><div key={v.id} className="flex items-center gap-3"><img src={v.image} alt="" width={80} height={60} loading="lazy" className="h-12 w-16 rounded-brand object-cover"/><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium text-ink">{v.name}</p><p className="font-mono text-[10px] text-muted">{v.registration}</p></div><span className={v.status==="available"?"text-xs text-ok":"text-xs text-brand"}>{v.status}</span></div>)}</div></section></div></main>}
