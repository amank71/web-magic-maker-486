import { createFileRoute, Link } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { ArrowRight, CalendarDays, CreditCard, MapPin } from "lucide-react";
import { Button } from "@/components/ui/button";
import { formatCurrency, sampleBookings, type DemoBooking } from "@/lib/rental-data";
export const Route = createFileRoute("/bookings")({ head: () => ({ meta: [{ title: "My Bookings — Velocity Fleet" },{ name: "description", content: "Review active, upcoming and completed Velocity Fleet rentals." },{ property: "og:title", content: "My Velocity Fleet bookings" },{ property: "og:description", content: "Rental dates, vehicle details and payment status in one place." },{ property: "og:type", content: "website" },{ name: "twitter:card", content: "summary_large_image" }] }), component: BookingsPage });
function BookingsPage(){const [bookings,setBookings]=useState<DemoBooking[]>([sampleBookings[0]]);useEffect(()=>{const raw=sessionStorage.getItem("velocity-demo-booking");if(raw){try{setBookings([JSON.parse(raw) as DemoBooking,sampleBookings[0]])}catch{setBookings([sampleBookings[0]])}}},[]);return <main className="mx-auto min-h-[75vh] max-w-5xl px-5 py-12 sm:px-6"><p className="eyebrow">Customer desk</p><div className="mt-2 flex flex-wrap items-end justify-between gap-4"><div><h1 className="font-display text-5xl text-ink">MY BOOKINGS</h1><p className="mt-2 text-muted">Signed in as Aarav Sharma · demo account</p></div><Button asChild className="bg-brand text-brand-ink hover:bg-brand/90"><Link to="/fleet">Book another car <ArrowRight /></Link></Button></div><div className="mt-9 space-y-4">{bookings.map((b)=><article key={b.reference} className="rounded-card border border-line bg-panel p-5"><div className="flex flex-col justify-between gap-5 sm:flex-row"><div><div className="flex flex-wrap items-center gap-3"><span className="font-mono text-xs text-brand">{b.reference}</span><span className="rounded-full bg-ok/15 px-2 py-1 font-mono text-[10px] uppercase text-ok">{b.status}</span></div><h2 className="mt-3 text-xl font-semibold text-ink">{b.vehicle}</h2><p className="mt-1 font-mono text-xs text-muted">{b.registration}</p><div className="mt-5 flex flex-wrap gap-x-6 gap-y-3 text-sm text-muted"><span className="flex items-center gap-2"><CalendarDays className="size-4 text-brand" />{b.startDate} — {b.endDate}</span><span className="flex items-center gap-2"><MapPin className="size-4 text-brand" />Connaught Place</span><span className="flex items-center gap-2"><CreditCard className="size-4 text-brand" />{b.paymentStatus}</span></div></div><div className="sm:text-right"><p className="text-xs text-muted">Booking total</p><p className="mt-1 text-2xl font-semibold text-ink">{formatCurrency(b.total)}</p><p className="mt-1 text-xs text-muted">Paid {formatCurrency(b.paid)}</p><Button variant="outline" className="mt-4 border-line bg-transparent text-ink hover:bg-paper">View details</Button></div></div></article>)}</div></main>}
